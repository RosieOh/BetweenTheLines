---
title: '[StoryG 프로젝트] SnapShot 설계: PRE/POST 응답 비교 가능한 데이터 모델'
date: '2026-04-14'
author: '오태훈'
description: 'StoryG 설문 시스템 사례로 보는 Snapshot 데이터 설계. PRE/POST 응답을 비교 가능한 형태로 저장하는 모델링과 구현 코드를 정리합니다.'
category: 'SpringBoot'
tags: ['Snapshot', '설문', '데이터모델링', 'StoryG', '백엔드']
readTime: '10분'
thumbnail: 'architecture'
series: 'storyg-realworld-series'
seriesOrder: 2
seriesLabel: '기본편 2'
---

# [StoryG 프로젝트] SnapShot 설계

리포트팀이 가장 많이 하던 질문은 "그래서 이 학생이 좋아진 건가요?"였습니다.  
데이터는 많았지만 PRE/POST 맥락이 없어서, 숫자는 보여도 변화는 설명할 수 없는 상태였습니다.

처음에는 ETL에서 보정해보려 했습니다. 하지만 소스 데이터가 `NULL`이면 후처리로 복원할 수 없었습니다.  
결국 문제의 중심은 분석 SQL이 아니라 저장 모델이었습니다. "제출 데이터"를 받는 구조에서 "의미 있는 스냅샷"을 남기는 구조로 바꿔야 했습니다.

이 글은 PRE/POST를 나중에 해석하는 방식이 아니라, 처음 저장할 때부터 해석 가능하게 만드는 설계 전환 과정을 다룹니다.

## 🕰️ 사건 타임라인

- 이슈 접수: 리포트팀이 전후 비교 불가 보고
- 원인 확인: `response_context` 일괄 `NULL`
- 계약 정리: 프론트/백엔드 컨텍스트 소스 합의
- 반영/검증: `isPrePost + stage` 기반 자동 파생 적용

## 🚨 문제

- `response_context`가 `NULL`로 저장되어 PRE/POST 비교 불가
- 동일 `questionId`의 시간축 변화 분석이 불가능
- 리포트/엑셀에서 전후 비교 지표를 만들 수 없음

## 🧪 재현

```sql
SELECT question_id, response_context, answer_rating
FROM tbl_answers
WHERE survey_response_id = ?;
```

```text
question_id | response_context | answer_rating
101         | NULL             | 4
101         | NULL             | 2
```

데이터는 있지만 의미가 없는 상태였습니다.

## 🔍 원인 분석

프론트와 백엔드의 컨텍스트 계약이 어긋났습니다.

- 프론트: 요청 최상단 `stage`에 PRE/POST 전달
- 백엔드: 각 answer의 `responseContext`에서 읽으려 함

즉 스냅샷 컨텍스트의 소스를 어디로 볼지 합의가 없었습니다.

## 🛠️ 해결

핵심은 섹션 플래그 + 제출시점 파생으로 분리한 것입니다.

1. `TemplateSection.isPrePost` 도입
2. `SurveyResponse.stage`를 기준으로 `Answer.response_context` 자동 파생

```java
AnswerContext resolvedContext = null;
if (question.getTemplateSection() != null && Boolean.TRUE.equals(question.getTemplateSection().getIsPrePost())) {
    if (surveyResponse.getStage() != null) {
        resolvedContext = switch (surveyResponse.getStage()) {
            case PRE  -> AnswerContext.PRE;
            case POST -> AnswerContext.POST;
            default   -> null;
        };
    }
} else {
    resolvedContext = AnswerContext.fromString(answerReq.getResponseContext());
}
```

### 스키마 포인트

```sql
-- 신규 플래그
tbl_template_sections.is_pre_post BOOLEAN DEFAULT false

-- 기존 컬럼 재활용
tbl_answers.response_context VARCHAR(20) NULL
```

## ✅ 검증

- isPrePost 섹션 문항은 PRE/POST가 안정적으로 저장
- 일반 섹션은 기존 동작 유지
- 프론트 요청 구조를 크게 바꾸지 않고 적용 완료

## 📌 교훈

1. Snapshot 데이터는 저장 여부보다 "컨텍스트 결정 규칙"이 핵심입니다.
2. enum을 늘리는 것보다 의미가 명확한 boolean 플래그가 더 나은 경우가 있습니다.
3. 분석 실패는 런타임 에러보다 늦게 드러나므로 계약 검증이 필수입니다.

## ♻️ 재사용 체크리스트

- [ ] 제출 컨텍스트(stage)와 질문 메타(isPrePost) 분리
- [ ] DB에서 PRE/POST 샘플 쿼리 자동 검증
- [ ] 프론트-백엔드 계약 문서에 컨텍스트 위치 명시
- [ ] 리포트팀과 필드 의미를 합의한 뒤 반영

## ✨ 마무리

이번 이슈를 통해 배운 건 단순합니다. 데이터는 "저장"이 아니라 "해석 가능성"까지 설계되어야 합니다.  
Snapshot이 필요한 도메인이라면, 지금 필드가 아니라 컨텍스트 결정 규칙부터 점검해보세요.

## 🔗 관련 글 / 다음 글

- 이전 글: [AI 프롬프트 엔지니어링: aiResult 적용기](/post/22)
- 다음 글: [Nginx 트러블슈팅: 502, 405, 리다이렉트 이슈 복구](/post/24)
- 심화편: [AI 리포트 프롬프트 운영 패턴 7가지](/post/25)
